dbt_medallion:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5433
      user: dbt_user
      password: dbt_password
      dbname: dbt_medallion
      schema: public
      threads: 4

